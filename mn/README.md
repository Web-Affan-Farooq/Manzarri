## Manzarri UI update:

### To be implemented:
- The header is switching markup , implement a functionality for smoothly sliding from left and right

### Prompt:
```markdown
## Ecommerce jwewllery website (Manzarri)
Your task has to create a ecommerce app . The UI contains the interfaces for the following pages
- / (home page or landing page) 
- /marketplace (mein product listing page) (I've placeholder data so dont create arrays and objects manually)
- /profile (user profile because users have thier accounts) 
- /profile/cart (carts page) 
- /profile/wishlist (wishlist page)
- /profile/settings (profiles settings)
- a section for displaying notifications  on account
- login page (only two input fields email and password) and button
- signup page (only three fields name  ,email , password)
- Create appropriate header and footer 

## Design assets :
**Color pallete**:  [`black (#11100F)` , `redish brown(#5D1c34)` , `lighter faun (#A67d44)`, `lighter green (#899481)` , `skin (#cdbcab)` , `custom white (#efe9e1)` ]

## Important notes :
- create a wining ui for this app which should imperess my client
- components must be responsive
- Dont crete actuual functionalities like cart wishlist etc , the states to be used only when highly needed like in header for controlling header in mobile ,
## Tech stack :
- Next.JS
- TailwindCSS 
```